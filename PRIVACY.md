# 隱私權說明

最後更新：2026-08-17

StreamCrate 是本機 Windows 應用程式，沒有遙測、帳號系統、廣告 SDK 或分析服務。

## 程式儲存的資料

應用程式會在 `%LocalAppData%\StreamCrate` 儲存設定、工具程式、輪替日誌與 SQLite 歷史資料。歷史可包含媒體 ID、已清理的來源網址、標題、格式、畫質、輸出路徑、結果、錯誤分類與時間。設定可包含下載資料夾、預設格式／畫質、語言、主題、Cookie 瀏覽器種類偏好與工具更新檢查時間。

## 不會儲存的資料

程式不會保存 Cookie 內容、授權標頭、cookies.txt 路徑、完整子程序命令列，或來源網址中可能包含 token 的查詢字串。Cookie 僅在本次操作期間保留於記憶體，並透過 yt-dlp 存取你已選擇的瀏覽器或檔案。

## 網路連線

程式只會為下列目的連線：

- 你要求的媒體資訊解析與下載。
- 首次準備或更新 yt-dlp 與 FFmpeg，且先驗證官方釋出的 SHA-256。
- 每 24 小時一次的 GitHub Release 更新檢查；本機沒有設定 `GITHUB_REPOSITORY` 時，此功能停用。

除上述情況外，程式不會將使用資料傳送給 StreamCrate 開發者。

## 你的控制權

你可在設定中變更下載位置、關閉或不執行手動工具更新，並使用歷史頁面管理本機紀錄。解除安裝時預設保留 `%LocalAppData%\StreamCrate`，讓你自行決定是否刪除資料與未完成的 `.part` 檔案。

## 內容與權利

請只下載你有權下載的內容。StreamCrate 不支援 DRM 規避，也不會繞過平台的存取控制。網站、媒體服務與第三方工具的資料處理另受其各自條款與隱私政策約束。
